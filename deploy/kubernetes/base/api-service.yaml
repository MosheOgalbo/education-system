apiVersion: v1
kind: Service
metadata:
  name: education-api
  namespace: education-system
  labels:
    app.kubernetes.io/name: education-system
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: education-system
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
